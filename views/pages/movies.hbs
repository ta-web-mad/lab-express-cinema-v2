<div class="container">
    <div class="row text-center">
        {{#each allMovies}}
        <div class="col-md-4 movies">
            <img src="{{image}}" alt="">
            <h3>
                {{title}}
            </h3>
            <a href="/movies/{{_id}}">See more</a>

        </div>
        {{/each}}
    </div>
</div>